/**
  ******************************************************************************
  * @file    main.c
  * @author  Auto-generated by STM32CubeIDE
  * @version V1.0
  * @brief   Default main function.
  ******************************************************************************
*/
#include <stdint.h>

int main(void)
{

	uint32_t *pClkctrleg = (uint32_t*)0x40023830;
	uint32_t *pPortDModeReg = (uint32_t*)0x40020C00;
	uint32_t *pPortDoutReg = (uint32_t*)0x40020C14;

	/**pClkctrleg = *pClkctrleg | 0x08;
	*pPortDModeReg = *pPortDModeReg & 0xFCFFFFFF;
	*pPortDModeReg = *pClkctrleg | 0x01000000; //setting
	*pPortDoutReg = 0x1000;*/

	//1. enable the clock for GPIOD peripheral in the AHB1ENR by setting the third bit
	*pClkctrleg = (*pClkctrleg | (1<<3));

	//2. configure the mode of the IO pin as output
	//a. clear the 24th and 25th bit position
	*pPortDModeReg = (*pPortDModeReg & ~(3<<24));
	//b. make 24th bit position as 1 (SET)
	*pPortDModeReg = (*pPortDModeReg | (1<<24));

	//3. set 12th bit of the output data register to make I/O pin-12 as HIGH
	*pPortDoutReg = (*pPortDoutReg | (1<<12));

	while(1){
		;
	}



	return 0;
}
